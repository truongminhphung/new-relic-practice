# Planning docs (not source)
IMPLEMENTATION.md
LAMBDA_FORWARDER_PLAN.md

# .NET build outputs
**/bin
**/obj

# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup

# Lambda build artifact (regenerated by terraform apply)
lambda/forwarder.zip
